# NOTE: currently not used, but shows how to generate SA models for use with Flask extension. Auto
# generated with `flask-sqlacodegen --flask postgresql://user@localhost/foo`

from sqlalchemy import Column, Date, Integer, Numeric, String, Table
from extensions import db